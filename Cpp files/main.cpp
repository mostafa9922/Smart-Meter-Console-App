#include "User Interface.h"
using namespace std;

int main()
{
    electrical_properites loads[num_of_loads];
    home_appliances loads_info[num_of_loads];
    showing_l( loads, loads_info);

   return 0;
}

